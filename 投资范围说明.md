# 投资范围说明

## 当前股票范围

从本版本开始，日常股票采集、财务续采和研究队列只覆盖投资者能够交易的沪深主板 A 股：

- 上海证券交易所主板；
- 深圳证券交易所主板；
- 上市状态为正常上市、ST 或 `*ST`。

明确排除：

- 北京证券交易所；
- 创业板；
- 科创板。

范围规则以 `config/investment_universe.json` 为准。代码同时检查交易所、证券代码前缀和来源返回的板块归属，避免只靠一种判断方式漏掉新代码段。

已有 Raw 快照可能包含上述被排除板块。它们是历史原始证据，按不可覆盖原则继续保留；范围调整只影响后续采集和后续生成的标准化／派生结果，不回写旧 Raw。

## 当前 ETF 范围

系统新增独立的境内 ETF 采集范围：

- 在上海或深圳交易所上市；
- 跟踪纳斯达克 100 或标普 500；
- 包括符合条件的境内跨境／QDII ETF；
- 不采集在美国等境外市场直接上市的 QQQ、SPY、VOO 等基金。

ETF 保存为独立的 `etf_snapshots` 标准化表，不进入个股财务报表、高级财务指标或个股筛选队列。原始跟踪指数名称和字段血缘仍会保留。

## 每日调用预算

`config/collection_budget.json` 以当前观察到的每日 100 次额度作为运行假设，最多主动使用 90 次并保留 10 次缓冲。交易日预算优先保证：

- 沪深主板行情最多 32 页；
- 纳指／标普相关 ETF 最多 2 页；
- P0 回购公告和 P0 公司新闻各最多 2 次；
- 中国宏观、全球宏观和行业政策新闻合计最多 3 次；
- 财务续采最多 32 页。

交易日全部任务的最大预算为 73 次。程序达到配额时必须停止并从已保存页续跑，不得绕过额度，也不得把不完整分页发布为完整数据。这个数字是运行预算，不代表数据源永久不变的官方承诺；额度变化时应先更新配置和测试。
