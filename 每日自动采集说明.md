# 每日自动采集说明

## 这套流程做什么

每天由 Codex 负责调度可用的金融数据、公告和新闻工具。工具返回的内容不会先交给模型改写，而是作为原始响应交给 Python：

```text
Codex 定时任务
→ 调用数据／公告／新闻工具
→ 生成临时采集产物
→ Python 安全导入
→ Raw 原始数据
→ 字段解析与标准化
→ 派生计算、报告和完整性检查
→ 验证通过后进入 GitHub
→ main 自动生成白名单公开快照并部署 GitHub Pages
```

Codex 只负责采集编排和故障处理，不在这条链路中评价公司、推荐股票或执行交易。

## 每日采集内容

具体清单位于 `config/codex_daily_collection.json`，当前包括：

- 交易日沪深主板 A 股市场快照（含正常、ST、`*ST`，排除北交所、创业板和科创板）；
- 境内上市、跟踪纳斯达克 100 或标普 500 的 ETF 快照；
- 最新 P0 研究队列公司最近七日股份回购公告；
- 最新 P0 研究队列公司最近七日重要新闻；
- 中国宏观与资本市场政策、全球宏观与海外市场事件、重要行业政策与产业事件；
- 尚未完成的版本化财务批次检查与续采。

周末没有新的交易日行情时，跳过 A 股行情和 ETF 快照；公告、新闻、尚未完成的财务批次续采和系统检查仍按计划执行。

详细证券范围及每日调用预算见 [`投资范围说明.md`](投资范围说明.md)，资讯边界见 [`新闻资讯范围说明.md`](新闻资讯范围说明.md)。ETF 使用独立标准化表，不会混入个股财务指标和个股研究队列。

公司级监控运行前先执行：

```bash
python3 scripts/resolve_monitoring_collection_scope.py --date YYYY-MM-DD
```

该命令从最新研究队列生成 P0 公司名单和实际查询；宏观、政策和行业路线不受 P0 限制。

## Raw 保护规则

Codex 必须把每项工具结果保存成临时 JSON 产物，包含：

- `query`：实际查询内容；
- `as_of_date`：数据对应日期；
- `fetched_at`：实际获取时间；
- `source`：真实数据来源；
- `raw_response`：未经修改的工具响应；
- `raw_field_names`：响应中的原始字段名；
- `collector`：本次使用的 Codex 工具。

Python 导入器会重新检查字段名是否与原始响应一致，并拒绝包含 Token、Cookie、密码或其他凭据字段的产物。通过检查后，原始响应先写入 `data/raw/`，且不会覆盖已有快照。

可标准化的分页财务采集产物还必须在顶层提供 `collection_job`，包含契约版本、计划任务 ID、请求版本和预期报告期；每个 `responses[]` 项必须提供 `collection_request`，包含契约版本以及实际请求的 `page` 和 `limit`。导入器严格校验后计算 `collection_job.query_sha256`，再把两者保存到每个 Raw envelope 的 metadata 中。这些字段是请求和计划的血缘，不属于数据源响应；不得把 `page`、`limit` 或其他血缘字段注入或回填到 `raw_response`。新导入的财务采集产物如果缺少这份契约，只能 Raw-only 隔离保存，不得进入标准化；已有历史 Raw 保持不可变。

沪深主板财务任务还必须达到计划中声明的最低证券覆盖数，并保证所有页面的证券代码全局唯一、唯一证券数与来源报告的总数一致。小样本、重复证券、页数不完整或报告期不符都会先保存 Raw，再以同版本失败指纹隔离，避免次日重复采集同一个坏响应。

市场、ETF 以及查询中声明了排序标记的财务任务，必须按 `config/codex_daily_collection.json` 中版本化的 `response_contract` 在 Raw 行顺序上验证，不能先排序再检查。三类问财响应都必须真实携带非空 `query`，并与采集产物的实际问句精确一致；启用排序契约时 Raw 还必须携带有效 `page`，若同时存在请求页码血缘则两者必须相等。每一页证券代码都要严格升序，相邻页面还必须满足上一页末项小于下一页首项；重复边界、倒序或页码血缘不一致均表示来源没有提供可证明的稳定快照，只能 Raw-only。仍按原身份保留的历史财务任务不会被新规则暗中改写；其查询下次发生变更时必须加入排序标记、提升 `request_version` 并保留上一版精确问句。ETF 还必须核对来源 `chunks_info` 的查询解析证据：纳斯达克 100、标普 500 和 ETF 三组语义都要存在，且不得出现“重仓概念”等已知错误语义。即使来源成功返回零行，语义证据未知或错配也不能记作合法空结果。

## 本地验证命令

仅检查临时采集产物，不写文件：

```bash
python3 scripts/import_codex_collection.py <采集产物.json> --dry-run
```

保存 Raw 并执行对应标准化：

```bash
python3 scripts/import_codex_collection.py <采集产物.json>
python3 scripts/run_daily_pipeline.py
python3 -m unittest discover -s tests
python3 scripts/validate_repository.py
python3 scripts/export_public_site.py --output-dir site/public/data
pnpm --dir site build
python3 scripts/validate_public_site.py site/dist
git diff --check
```

每次导入会在 `reports/daily/codex-collection-runs/` 生成一份中文命名的 `每日采集导入审计.json`。审计只保存仓库相对路径和处理结果，不保存凭据或本机路径。

仓库大小门控只检查 Git 已跟踪文件和未被忽略的未跟踪文件，也就是可能进入发布的候选集。已被 `.gitignore` 排除且从未跟踪的 `.next/`、`.pnpm-store/` 等本地缓存不会触发门控；已跟踪或未忽略的文件只要大于等于 100 MiB，仍必须阻止发布。

所有验证通过后，发布前仍必须核对当前分支与上游、`git status --short` 和准备暂存的精确文件。每日公开发布的规范分支必须是 `main`，并跟踪既有 `origin/main`；不得由功能分支冒充每日发布入口。只能发布本轮生成的公开数据、报告和审计；无法与既有人工修改或历史未发布产物明确区分时，停止发布。

公开站是确定性构建结果，不单独提交 `site/public/data/` 或 `site/dist/`。经验证的数据提交推送到 `main` 后，Pages 工作流会自动重新导出和部署；不需要人工重做网站。页面必须分别呈现管道执行状态、来源 artifact 状态和数据就绪度。任一站点导出、构建或专项隐私校验失败时，本轮不得推送，或由 GitHub Actions 停止部署并保留线上上一成功版本。

## 失败时如何处理

- 工具不可用或达到配额：记录失败，不编造数据；
- 当日调用达到安全预算：停止新增请求，保留 10 次缓冲并从已保存页续跑；
- 最新 P0 队列缺失或损坏：停止公司级新闻／公告采集，不得退回全市场公司查询；
- 分页不完整：`page`、`limit`、来源报告总数缺失或不一致，页集合不完整，或唯一记录数与总数不符时，只保留已取得的 Raw，不发布成完整标准化批次；
- 分页顺序不稳定：必须直接按 Raw 页内顺序验证证券代码严格升序，并验证相邻页边界严格递增；重复、倒序或页码血缘不一致时只保留 Raw，不局部补页、不去重伪造完整批次；
- ETF 查询语义错配：来源 `chunks_info` 未同时证明目标指数和 ETF 语义，或出现“重仓概念”等禁用语义时，只保留 Raw 并阻止标准化；零行不能覆盖这道检查；
- 市场口径异常：本轮符合 `config/investment_universe.json` 的证券数低于 `stocks.minimum_expected_count` 时，即使来源声称分页完整也必须停止标准化；
- 出现未知的核心／必需字段：保留原始字段，确定性更新映射规则、提升 `mapping_version` 并增加测试，然后从已有 Raw 重放；不重采、不猜测或回填数据。无明确等价语义的可选附加字段继续记录在 `unmapped_fields`，不得为消除未知字段而强行映射；
- 标准化或验证失败：停止 GitHub 发布，保留可追溯的错误信息；
- 无法明确区分本轮文件与既有修改：保留可恢复结果，停止暂存、提交和发布；
- GitHub 推送失败：保留本地结果，下次先检查差异再继续，不强制推送、改写历史或切换到未经确认的分支。
- Pages 导出、构建或 artifact 校验失败：不发布不完整网站，不复制 Raw 规避门控；修复后从同一已验证 Git commit 重新构建。
