# 仓库隐私与公开数据规则

本仓库以公开、可复核的投资研究为目标。版本控制边界按“内容是否涉及隐私”划分，而不是按文件大小、目录类型或数据重要性划分。

## 应当提交的内容

以下内容对分析、复现或审计有价值，应正常提交：

- 不含个人身份信息或凭据的市场原始数据；
- 标准化数据、衍生数据和数据血缘；
- 字段映射、非敏感项目配置和配置模板；
- 数据处理脚本、测试和验证报告；
- 已脱敏的研究材料、组合分析和决策复盘。

公开审计、清单、报告及其他持久化元数据中的仓库文件路径，必须使用以仓库根目录为基准的 POSIX 相对路径。用户主目录等本地绝对路径属于私密且与机器绑定的元数据，不得提交。

不得为了减小仓库体积而笼统忽略 `data/raw/`、`data/normalized/`、`data/derived/`、`reports/` 或其他核心分析目录。

## 仅保留在本地的内容

以下内容不得提交到公开仓库：

- API 密钥、访问令牌、Cookie、登录会话、密码和私钥；
- 身份证号、手机号、住址、银行卡号、券商资金账号等个人身份或账户信息；
- 未脱敏的个人持仓、成交流水、券商对账单、税务文件；
- 明确标记为私密的研究或决策记录。

私密输入统一放在任意目录下的 `private/` 子目录，或使用 `*.private.*` 文件名。例如：

```text
portfolio/private/holdings.csv
decision_journal/private/2026-08-08.md
config/local.toml
```

需要公开分析结果时，应移除个人标识和账户标识后，将脱敏或汇总结果保存到普通目录。不要将整个 `portfolio/` 或 `decision_journal/` 排除，因为其中可以包含对研究有价值且适合公开的内容。

## 提交前检查

`.gitignore` 只能阻止尚未被跟踪的文件，不能保护已经被 Git 跟踪或已经进入 Git 历史的秘密。仓库验证器以 Git 已跟踪文件和未被忽略的未跟踪文件作为发布候选；它会检查候选文件大小、私密路径、本机绝对路径，并对 JSON、JSONL 和 CSV（包括 `data/raw/` 与审计产物）执行统一的凭据、登录会话、个人身份和账户字段安全检查。被忽略的本地缓存不会阻断发布，但已经跟踪的同名文件仍会被检查。提交前应检查：

```bash
git status --short
git diff --cached
```

如发现凭据已经被提交，应先撤销或轮换凭据，并在仓库公开前清理 Git 历史。

旧受控采集工作流曾预留 `IWENCAI_API_KEY` GitHub Secret；该入口目前不属于每日运行路径。任何未来数据源密钥都只能保存在 GitHub Secret 或本机安全环境中，不能写入工作流、配置、日志、Raw 响应或 Actions artifact。采集审计只能记录凭据是否存在，不能记录凭据值。

Codex 每日采集产物同样不得包含 Token、Cookie、Authorization 请求头、密码或其他凭据字段。临时收件箱 `.codex-collection-inbox/` 不进入 Git；采集器必须原样保存实际收到的完整已安装 skill 输出，不得绕过 skill 直接请求数据提供方 OpenAPI。只有完整输出本身不含敏感认证或账户上下文、且经过统一安全门与导入器验证的 Raw 快照和便携审计文件才能进入公开仓库。若命中安全门，完整响应只能留在忽略的收件箱或 `private/` 隔离区，不得删除字段后伪装为原始响应。

下载的采集产物不得直接复制进仓库。专用导入器必须先验证凭据排除标记、Raw 哈希、查询日志和路径边界；导入报告不得保存 Actions 下载目录或其他本机绝对路径。
