# 新闻资讯范围说明

## 市场级资讯：保持广泛覆盖

以下信息每天按市场级主题采集，不受 P0 公司名单限制：

- **中国宏观与资本市场政策**：国务院、人民银行、财政部、证监会、发改委的重要政策，以及可能影响 A 股的重大宏观事件；
- **全球宏观与海外市场**：美联储、美国通胀与就业、利率与汇率、关税与贸易、地缘政治，以及影响纳斯达克和标普 500 的重大事件；
- **行业政策与产业事件**：重要产业政策、行业监管、产业扶持和重大技术产业变化。

上述路线默认关注最近三日，每条查询请求最多返回 20 项结果。它们负责收集事实，不在采集阶段判定“利好”或“利空”。政策和事件对市场、行业或 ETF 的影响由后续 ChatGPT 研究层判断。

“请求最多 20 项”不等于数据源一定返回所有候选结果。覆盖验收会单独记录接口是否成功、实际返回数、数据源报告的总数、来源分布和 P0 归属情况。只要存在已知截断或 P0 无法归属，系统就标记为覆盖不足，不会把“接口调用成功”误报为“资讯完整”。

## 公司级资讯：只关注最新 P0

以下两类公司信息不再面向全部沪深主板公司采集：

- P0 公司最近七日重要新闻；
- P0 公司最近七日股份回购公告。

运行前，`scripts/resolve_monitoring_collection_scope.py` 会读取最新的主板研究队列，只提取 `priority = P0` 的证券代码和名称，并分批生成查询。每个 Raw 快照同时保存：

- P0 证券代码清单；
- 产生该名单的研究队列清单路径；
- 研究队列对应日期；
- 允许的公告类型。

即使搜索结果夹带非 P0 公司，Raw 响应仍会完整保存，但标准化层会排除范围外记录。回购公告还会再次检查事件类型，只保留 `share_repurchase`。

查看当天实际监控名单和查询：

```bash
python3 scripts/resolve_monitoring_collection_scope.py --date YYYY-MM-DD
```

如果最新研究队列不存在、P0 为空、清单哈希不正确或 P0 数量超过配置上限，任务会明确失败，不会自动退回全市场公司采集。

## 每日调用预算

交易日最大预算现在是 73 次，仍低于 90 次安全上限并保留 10 次总额度缓冲。其中资讯监控最多使用：

- P0 回购公告：2 次；
- P0 公司新闻：2 次；
- 三条宏观／政策／行业新闻路线：3 次。

这些是最大值；当 P0 数量不超过 25 家时，公司新闻和回购公告通常各只需 1 次。

## 2026-08-09 真实覆盖验收

同花顺问财的连接和鉴权均正常，五条查询也都成功返回。但返回覆盖不完整：

- P0 回购公告：返回 0 / 报告总数 0；
- P0 公司新闻：返回 2 / 报告总数 9，两条均缺少证券归属，经 P0 二次过滤后为 0；
- 中国宏观与政策：返回 2 / 报告总数 20；
- 全球宏观与海外市场：返回 13 / 报告总数 20；
- 行业政策与产业事件：返回 1 / 报告总数 20。

因此，目前可以确认“能正常获取资讯”，但不能确认“能获取完整资讯”。验收原始响应、标准化结果和覆盖报告均已保留，且 P0 范围外或无法归属的记录没有进入正式 P0 监测报告。
