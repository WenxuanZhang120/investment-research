# 系统架构与职责边界

## 1. 系统定位

本项目是 **AI 辅助的个人投资研究与决策支持系统**，不是自动交易系统。

系统负责可靠地准备事实、组织研究并支持复盘，但不会代替投资者作出或执行投资决定。以下职责边界是后续开发、自动化和研究流程必须遵守的架构约束。

## 2. 核心角色

| 角色 | 核心职责 | 不承担的职责 |
| --- | --- | --- |
| Codex | Build / Fix / Improve：建设、测试、维修和升级系统 | 日常数据运行、投资判断、交易决策 |
| Python + GitHub Actions | Run：确定性执行采集、保存、解析、标准化、计算和验证 | 临时猜测字段含义、投资分析 |
| iWencai | 提供 A 股等投资数据 | 数据标准化、投资结论 |
| GitHub | 保存代码、公开数据、版本、运行记录和审计证据 | 保存凭据或未脱敏个人数据 |
| ChatGPT | Analyze：研究、解释、比较、风险审查和提出建议 | 底层数据工程、自动下单、最终决策 |
| 投资者 | 审查证据并作出最终投资决策 | 将最终权限交给自动化系统 |

最简洁的分工是：

> Codex 负责可靠地准备事实，ChatGPT 负责解释事实，投资者负责最终决策。

## 3. 目标架构

```text
Codex（开发 / 修复 / 升级）
              │
              ▼
Python + GitHub Actions（自动运行）
              │
              ▼
iWencai → Raw → 解析 → Normalized → Derived → 验证
                                              │
                                              ▼
                                      GitHub 中央仓库
                                              │
                                              ▼
                                  ChatGPT（研究 / 风险审查）
                                              │
                                              ▼
                                      投资者最终决策
```

Codex 和 ChatGPT 都不应成为日常数据流水线的必需运行组件。系统稳定后，日常采集和处理应由普通 Python 程序独立完成。

## 4. Codex 的工程边界

Codex 可以负责：

- 项目架构、Python 代码和测试；
- iWencai 接入、动态字段解析和字段映射；
- raw-first 存储、标准化、衍生计算和数据质量检查；
- GitHub Actions、仓库验证、故障诊断和系统升级；
- 将反复出现的问题固化为确定性规则。

Codex 不应回答或决定：

- 某家公司是否值得买入；
- 是否加仓、减仓或卖出；
- 财报整体是好是坏；
- 投资逻辑是否成立或已被破坏；
- 当前估值是否合理；
- 投资组合应如何调整。

工程代码可以计算预先定义且可审计的指标，但不得把计算结果自动解释为投资建议。

## 5. ChatGPT 的研究边界

ChatGPT读取已经标准化、验证且可追溯的数据，负责：

- 公司基本面、财报和盈利质量分析；
- 估值、行业比较、公告和新闻影响分析；
- 风险识别、投资 thesis、红队审查和持仓复盘；
- 个股比较、组合风险检查和投资委员会式审查；
- 向投资者提出有依据、可质疑的建议。

ChatGPT 的分析必须区分事实、推断和判断，并尽可能指向可追溯的数据或来源。它不负责修复底层数据，不执行交易，也不拥有最终决策权。

## 6. 日常运行不得依赖 LLM

稳定运行阶段的目标流程是：

```text
定时触发
  ↓
Python 调用数据源
  ↓
先保存不可变 Raw 快照
  ↓
解析与标准化
  ↓
计算确定性指标
  ↓
完整性、血缘和隐私验证
  ↓
发布可审计结果或明确失败
```

这条主路径应做到零 Codex Token，并且不因 Codex 或 ChatGPT 暂时不可用而停止。LLM 只用于建设、维修、扩展和研究，不用于重复执行已经能够写成规则的工作。

仓库以 `scripts/run_daily_pipeline.py` 作为统一编排入口，以 `config/daily_pipeline.json` 保存版本化阶段和命令。编排器不使用 shell，不保存子进程输出正文或本机路径，任何步骤失败都会停止后续阶段并生成可审计状态。

执行前由 `scripts/resolve_daily_pipeline.py` 建立本地输入就绪门控。它只接受财务采集计划中完整、无分页错误的 Raw 批次，并使用原始记录 ID 与处理版本识别对应的标准化和衍生批次。未完成输入被明确标记为等待，相同身份的已有输出被标记为最新；只有完整且尚未处理的批次会自动加入标准化和衍生阶段。当前外部采集开关关闭，因此这个门控本身不会补采数据，也不会调用 LLM。

GitHub Actions 的 `daily-offline-pipeline.yml` 只负责定时或手动运行这条离线路径，并将运行记录保存为有限保留期的 Actions artifact。工作流使用仓库只读权限，不提交生成文件，也不持有数据源凭据。外部采集必须在独立任务中经过安全、配额和失败恢复审查后才能启用。

外部财务采集使用独立的 `manual-financial-collection.yml`，不得由日常定时流水线间接调用。人工入口默认只执行无网络预检；实际采集要求任务白名单、精确确认文本、GitHub Environment、仅在采集步骤可见的 Secret、单次页数预算和不可并发执行。每个已验证响应先写入不可变 Raw，再进行跨页检查；即使后续页失败，已保存的 Raw 与查询日志也会打包为审计产物，不能因整批失败而丢弃。

## 7. 动态字段处理

iWencai 通过自然语言查询返回动态字段。字段可能包含日期、报告期、复权方式、单位或动态后缀，因此必须经过版本化解析和映射。

每次采集至少应保留：

- 原始响应与原始字段名；
- 标准字段名；
- `as_of_date` 或 `period_end`；
- `fetched_at` 与可得时间；
- 数据来源、解析器版本和映射版本。

遇到未知字段时必须：

1. 保存原始数据；
2. 将字段记录到待处理集合；
3. 不猜测、不静默丢弃、不自动调用 LLM；
4. 由 Codex 后续审查并更新一次确定性映射规则；
5. 使用保存的原始数据重新处理。

## 8. 数据与结论必须可追溯

任何重要分析应能沿以下链路向下追溯：

```text
分析结论
  ↓
衍生指标与标准化数据
  ↓
原始快照
  ↓
数据来源、查询和获取时间
```

Raw 数据先于标准化保存且不得覆盖。标准化和衍生数据必须记录来源批次、原始字段、适用期间和处理版本。历史查询必须遵守时点可得性，避免使用当时尚未公开的数据。

## 9. GitHub、隐私与公开边界

GitHub 是代码、公开研究数据、配置、测试、报告、运行记录和审计证据的中央仓库。仓库内路径必须使用可移植的相对路径。

API 凭据、登录会话、身份与账户信息、未脱敏持仓和交易流水不得进入公开仓库。`.gitignore` 不能保护已经被 Git 跟踪的文件；具体规则以 [`PRIVACY.md`](PRIVACY.md) 为准。

## 10. 失败与变更原则

- 未知字段或不完整数据应明确失败或标记缺失，不能猜测补全；
- 原始快照不可覆盖，新的解析规则应重放已有 Raw 数据；
- 映射、公式、解析器和计算器变化必须提升版本并增加测试；
- 数据采集、数据判断和投资判断必须保持分层；
- 自动化可以生成研究材料，但不能自动生成或执行交易指令；
- 最终权限始终属于投资者。

## 11. Token 与模型使用原则

Token 优化顺序是：

1. 消除不必要的 LLM 调用；
2. 将日常采集、清洗和检查改为确定性自动化；
3. 减少 Codex 重复读取和重复推理；
4. 最后再比较不同工程模型完成同一可靠任务的总成本。

模型评估应比较代码质量、调试能力、多文件修改稳定性、测试通过率、一次完成概率和任务总成本，而不是只比较每百万 Token 的单价。

理想消耗模式是：建设期较高，稳定运行期大幅下降，后期只在故障和扩展时出现维护峰值。
